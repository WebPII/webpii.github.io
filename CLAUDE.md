# Agent Notes

Use the existing Python virtual environment or create one with `python3 -m venv`.
Keep generated image/model/data directories out of version control unless they
are intentionally part of `example_data/`.

When changing methodology, update the relevant README or release note so the
review workflow remains reproducible.
